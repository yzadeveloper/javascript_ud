//Implementar la función validar, para que valide si la edad pasada como parámetro 
//esta entre 10 y 15 años. Guardar el resultado en la variable bool:
let bool;

function validar(edad) {
    bool = edad >=10 && edad <= 15;
    
}

//mplementar la función validar, que valide si la edad enviada por parámetro es 
//igual a 10 o igual a 20. Guardar el resultado en la variable bool:
let bool1;

function validar(edad) {
    bool1 = edad == 10 || edad == 20;
    
}